version : "2"
sql:
  - schema: "migrations/"
    queries: "internal/sql/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_interface: true
